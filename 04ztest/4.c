#include <stdio.h>
int main()
{
    int nums[5], i, j;

    printf("enter 5 number: ");
    for (i = 0; i < 5; i++)
    {
        scanf("%d", &nums[i]);
    }
    printf("your entered array is[");
    for (i = 0; i < 5; i++)
    {
        printf("%d ", nums[i]);
    }
    printf("]");

    for (i = 0; i < 5; i++)

    {

        if ()
            printf(" TRUE", nums[i]);
    }

    0 for (j = 0; j < 5; j++)
    {
        if ()
            printf("FALSE %d", nums[i]);
    }

    return 0;
}
